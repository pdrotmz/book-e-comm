spring:
  application:
    name: book-e-comm

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    database: h2

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

